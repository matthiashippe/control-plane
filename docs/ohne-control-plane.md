# Keeping an automaton running without a control plane

This page has moved and is now in English, where its readers are:

**→ [docs/without-control-plane.md](without-control-plane.md)**

It covers why the runtime stops thinking entirely when it cannot reach a balance endpoint, and the
three free ways around that: a local Ollama model (including the config field that silently defeats
it), writing the cached balance yourself to use your own OpenAI key, and a fork that has already
packaged the first route.

Diese Seite liegt jetzt auf Englisch unter
[without-control-plane.md](without-control-plane.md), weil ihre Leser Englisch sprechen.
